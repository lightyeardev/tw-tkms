# Fork versions carry an `-ly.N` suffix. Upstream has already tagged v0.36.0, so a plain 0.36.0 here could one day collide with a real Central
# artifact under identical coordinates, resolving by repository order. Consumers pin this version explicitly, so the suffix only has to be unique --
# it does not have to sort above upstream's.
version=0.36.0-ly.1
